import { TodoService } from '../Model/TodoService';
import { Map } from 'core-js';

export const Services = new Config();

services.add( )


export const services = {
		TodoModel : {
			api : '/api/v1/todos',
			endpoints: {
				create: '/new'
			} 
		}


}


